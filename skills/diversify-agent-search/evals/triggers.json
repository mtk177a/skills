{
  "skill": "diversify-agent-search",
  "version": 1,
  "run_policy": {
    "protocol": "Present each prompt as a Skill-selection task using only the installed names and descriptions declared for the condition. Open every selected SKILL.md so loading is observable, report selected names, and do not solve the underlying task.",
    "count_only_observed_skill_loads": true,
    "unobservable_result": "not exposed",
    "repetition_rule": "Run each affected case once per condition, then repeat only when variation, an unexpected result, model differences, or failure impact makes another observation decision-relevant.",
    "verdict_rule": "Record pass, fail, unstable, not exposed, or not executed for every case. Mixed observed results are unstable, not a pass."
  },
  "coexistence_skills": [
    "break-failure-loop",
    "design-changes",
    "implement-changes"
  ],
  "cases": [
    {
      "id": "exhausted-anchor-candidate-search",
      "expected": "should_trigger",
      "expected_handler": "diversify-agent-search",
      "category": "core",
      "prompt": "The current prompt-and-regex design anchor is exhausted and no local check will distinguish another wording variant. Create multiple structurally different candidates and compare them by evaluation case."
    },
    {
      "id": "case-level-candidate-search",
      "expected": "should_trigger",
      "expected_handler": "diversify-agent-search",
      "category": "core",
      "prompt": "Our average score hides that different candidates win on different case types. Preserve the candidate archive, define diversity axes, and select the next structural branch."
    },
    {
      "id": "reconstruct-only",
      "expected": "should_not_trigger",
      "expected_handler": "break-failure-loop",
      "category": "near_miss",
      "prompt": "Two equivalent edits failed under the same hypothesis. Pause without generating alternatives, reconstruct the evidence, and identify one diagnostic that could resolve the loop."
    },
    {
      "id": "missing-input-only",
      "expected": "should_not_trigger",
      "expected_handler": "break-failure-loop",
      "category": "near_miss",
      "prompt": "We may be repeating the same approach, but the attempt logs and current target are missing. Do not propose candidates; identify what evidence is needed before continuing."
    },
    {
      "id": "reframe-then-diversify",
      "expected": "should_trigger",
      "expected_handlers": [
        "break-failure-loop",
        "diversify-agent-search"
      ],
      "category": "coexistence",
      "prompt": "First reconstruct the repeated attempts and confirm whether the design anchor is exhausted. If no local distinguishing check remains, generate structurally different candidates with a protected evaluation matrix."
    },
    {
      "id": "first-implementation-failure",
      "expected": "should_not_trigger",
      "expected_handler": "implement-changes",
      "category": "near_miss",
      "prompt": "The focused test failed on the first implementation attempt. Diagnose the Red and make the smallest approved implementation change."
    }
  ]
}
