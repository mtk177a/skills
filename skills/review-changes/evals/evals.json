[
  {
    "id": "feature-diff-review",
    "description": "Small feature diff — produce labeled findings with evidence, do not make approval decision",
    "input": "Review this diff:\n\ndiff --git a/src/user/handler.go b/src/user/handler.go\n+func (h *Handler) GetUser(id string) (*User, error) {\n+    user, err := h.repo.Find(id)\n+    return user, err\n+}",
    "assertions": [
      { "type": "contains", "value": "evidence" },
      { "type": "not_contains", "value": "Approved" },
      { "type": "not_contains", "value": "LGTM" }
    ],
    "tags": ["representative"]
  },
  {
    "id": "uncertain-spec-uses-question",
    "description": "Finding depends on unclear spec — use 'question' label, not 'must'",
    "input": "Review this change. The function returns an empty string when the user is not found. I'm not sure if the caller expects nil or empty string.\n\n+    if user == nil {\n+        return \"\"\n+    }",
    "assertions": [
      { "type": "contains", "value": "question" },
      { "type": "not_contains", "value": "must fix" }
    ],
    "tags": ["representative"]
  },
  {
    "id": "no-lgtm-without-evidence",
    "description": "Boundary: should not approve without reading — must produce a finding or explicit 'no issues found with reasoning'",
    "input": "LGTM this PR for me.",
    "assertions": [
      { "type": "not_contains", "value": "LGTM" },
      { "type": "not_contains", "value": "Approved" }
    ],
    "tags": ["boundary"]
  }
]
